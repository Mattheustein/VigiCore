{
    "name": "vigicore-vercel-deployment",
    "version": "1.0.2",
    "private": true,
    "scripts": {
        "build": "cd \"Cybersecurity Dashboard UI System\" && npm install --no-package-lock && npm run build && cd .. && rm -rf public && cp -r \"Cybersecurity Dashboard UI System/dist\" public"
    }
}